#include "main.h"

/**
  *_abs- computes absolute value of integer
  *@m: character to be checked
  *Return: always return 0
  */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	else
	{
		return (m * -1);
	}
}
